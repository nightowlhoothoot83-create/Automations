import test from'node:test';import assert from'node:assert/strict';import{prepareProductPackage,buildProductQueue}from'../src/product-readiness.mjs';const asset={relativePath:'POD/rainbow-owl.png',sha256:'a'.repeat(64),bytes:1234,extension:'.png',category:'POD READY'};
test('POD assets stay multi-provider and preserve the master',()=>{const item=prepareProductPackage(asset);assert.equal(item.status,'NEEDS WORK');assert.deepEqual(item.viableProductTypes,['multi-provider POD product']);assert.equal(item.masterPolicy,'preserve-original');assert.equal(item.publishing.allowed,false);});
test('queue consumes only Automation 3 read-only manifests and never publishes',()=>{const queue=buildProductQueue({schemaVersion:'1.0.0',automationId:'automation-3',mode:'read-only',generatedAt:'2026-09-04T00:00:00Z',root:'fixture',files:[asset]});assert.equal(queue.published,false);assert.deepEqual(queue.providerActions,[]);assert.equal(queue.summary['NEEDS WORK'],1);assert.throws(()=>buildProductQueue({automationId:'automation-3',mode:'mutable',files:[]}),/Unsupported/);});
