# Connections Required

Last verified: 2026-08-22. Discovery was read-only. No token values, credentials, or live configuration are stored here.

| Integration | Purpose | Exact repository, service, or account required | Information or permission required | Testing or production | Current status | Next action once connected |
|---|---|---|---|---|---|---|
| GitHub account | Repository, workflow, build, and deployment-status monitoring | Verified account `nightowlhoothoot83-create`; 19 public repositories are inventoried in `config/discovery/github-repositories.json` | Select which repositories Automation 6 owns; use `AUTOMATION_GITHUB_TOKEN` only through the runtime environment | Testing and production visibility | Existing `gh` session verified; target selection pending; this repository has no remote | Copy the example connections file locally, set `settings.repository` and optional workflow, enable the profile, then inject the token at runtime |
| Existing GitHub workflows | Reuse current AdSense, indexing, CI, and build signals | Verified active workflows in `Mycalctools`, `Mycalendartools`, `Wheelnamepicker`, `ADG-MONITOR-V4`, `Raven-Sharp-Content-Creator`, and `Raven-Sharp-Book-Creator` | Confirm which workflow names are authoritative and whether read-only Actions API access is sufficient | Testing and production visibility | Workflow paths discovered; no calls or changes made | Add one connection profile per selected repository/workflow and run the adapter in dry-run before enabling polling |
| Automation repository remote | Run the new scheduled workflow and preserve artifacts | This local `automations` Git repository; destination GitHub repository is not yet identified | Remote repository URL and permission to push the feature branch; merging remains approval-gated | Testing | Local branch and commits ready; no remote configured | Add the chosen remote, push `codex/automation-6-infrastructure`, verify Actions on the branch; request approval before merge |
| Railway | Discover SaaS back ends and monitor service/deployment health | Railway account, workspace, project, environment, and service IDs are not locally discoverable | Existing authenticated Railway session or read-only token; exact workspace/project selection | Testing and production visibility | Railway CLI not installed; no nearby Railway config found | Install/authenticate Railway tooling or supply environment-referenced credentials, run read-only discovery, then populate connection settings |
| Cloudflare | Discover and monitor front ends, Workers, Pages, routes, and deployments | Cloudflare account plus selected Worker/Pages project IDs; none verified locally | Existing authenticated Wrangler session or scoped read-only API token in `AUTOMATION_CLOUDFLARE_API_TOKEN` | Testing and production visibility | Wrangler not installed; no nearby Wrangler config found | Install Wrangler v4, authenticate read-only, run `wrangler whoami`, then populate account/project settings; live deploy remains approval-gated |
| Website targets | Health, E2E, broken-link, screenshot, SEO, and AdSense checks | Exact staging/production URLs per selected system; only `https://openacm.jsonproductions.com/` is verified from GitHub metadata | Target list, environment classification, expected status/content, and any safe test-user requirements | Testing and production visibility | Example target works; real target inventory incomplete | Add targets to a local config copied from `config/targets.example.json`; keep authentication in environment references |
| Management Hub | Consume run status, evidence, approvals, and connection readiness | Hub endpoint or local consumer; current local source is `artifacts/hub/*.json` | Destination endpoint/schema owner and optional `AUTOMATION_HUB_TOKEN` runtime variable | Testing first, production later | Versioned local indexes implemented; remote destination unknown | Validate the v1 schemas, configure endpoint, exercise with a mock server, then enable authenticated delivery after approval if production-mutating |
| Logging and artifact storage | Retain reports, JSONL events, screenshots, and build evidence | Local paths now; optional future object-storage/logging destination | Retention rules, storage endpoint/bucket, write permission via runtime environment | Testing and production | Local ignored storage operational; GitHub artifact retention set to 30 days | Select storage adapter and retention; test writes against local/mock storage before enabling remote delivery |
| Screenshot renderer | Capture browser evidence | Local Playwright-compatible renderer or approved CI browser | Browser runtime choice, viewport matrix, authentication state policy | Testing and production visibility | Request records implemented; renderer not installed | Configure a local renderer command, test against fixtures, then enable selected public/staging targets |
| Notifications/monitoring endpoint | Deliver failure and regression events | Webhook/incident/observability endpoint not yet identified | Endpoint, event contract, severity routing, runtime credential reference | Testing and production | Configurable integration type exists; endpoint absent | Test against a local mock receiver, verify redaction/retry behavior, then activate the selected service |
| POD image generation | Generate production-quality POD artwork and complete product previews | Replacement provider is not yet confirmed; owner recalls it may be fal.ai, replacing or supplementing the current Runware path | Confirm provider/model/API endpoint, quality criteria, safe test quota, and runtime credential environment-variable name | Testing first, production later | Provider decision and credentials pending; preview and output-quality failures are known | Keep provider configurable, validate against mocks, then run test-mode quality and preview checks after the owner confirms the provider and supplies credentials securely |
| SaaS webhooks | Verify backend/Stripe/provider event delivery for all six products | Product-specific webhook endpoints and event inventories are not yet mapped | Endpoint identifiers, expected event types, signature-secret environment-variable names, safe replay/test method | Testing and production | Required by contract; connection details pending | Use local signed-event fixtures first, then test provider sandbox delivery without mutating live production |

## Activation rules

1. Copy `config/connections.example.json` to an ignored local file such as `config/connections.local.json`.
2. Fill in non-secret identifiers and endpoints. Keep `credentialEnv` as the environment-variable name; never put the value in JSON.
3. Run `node src/connections-cli.mjs --config config/connections.local.json` to validate readiness without calling live APIs.
4. Exercise adapters against fixtures or local mock servers, then explicitly enable read-only polling.
5. Treat merge to main/production, live Cloudflare deployment, production DNS/secrets, destructive production operations, and live production mutation as approval-gated actions.
