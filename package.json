{
  "name": "automation-operations-infrastructure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --test",
    "automation:6": "node src/cli.mjs",
    "orchestrate:dry-run": "node src/orchestrate.mjs",
    "orchestrate": "node src/orchestrate.mjs --execute",
    "connections:check": "node src/connections-cli.mjs",
    "policy:check": "node src/policy-cli.mjs",
    "visual:check": "node src/visual-gate-cli.mjs",
    "compare:dry-run": "node src/deployed-branch-compare-cli.mjs",
    "pod:preflight:fixture": "node src/pod-preflight-cli.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "pixelmatch": "^7.2.0",
    "playwright": "^1.62.1",
    "pngjs": "^7.0.0"
  },
  "dependencies": {
    "@fal-ai/client": "^1.10.1"
  }
}
