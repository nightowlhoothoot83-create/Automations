import { writeFile, mkdir } from 'node:fs/promises';import path from 'node:path';import { inventoryAssets } from './asset-inventory.mjs';
const args=process.argv.slice(2),root=args[args.indexOf('--root')+1],output=args[args.indexOf('--output')+1]||'artifacts/automation-3/inventory-v1.json';if(!root)throw new Error('Use --root with an explicit directory; broad drive discovery is never implicit');const report=await inventoryAssets(root);await mkdir(path.dirname(output),{recursive:true});await writeFile(output,`${JSON.stringify(report,null,2)}\n`);console.log(JSON.stringify({output:path.resolve(output),...report.summary,mode:report.mode}));
