import{readFile,writeFile,mkdir}from'node:fs/promises';import path from'node:path';import{buildProductQueue}from'./product-readiness.mjs';const args=process.argv.slice(2),input=args[args.indexOf('--inventory')+1],output=args[args.indexOf('--output')+1]||'artifacts/automation-4/product-queue-v1.json';if(!input)throw new Error('Use --inventory with an Automation 3 manifest');const queue=buildProductQueue(JSON.parse(await readFile(input,'utf8')));await mkdir(path.dirname(output),{recursive:true});await writeFile(output,`${JSON.stringify(queue,null,2)}\n`);console.log(JSON.stringify({output:path.resolve(output),...queue.summary,published:queue.published}));
